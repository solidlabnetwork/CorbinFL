A collection of 70,000 grayscale images (28x28 pixels) of handwritten digits (0-9). It is commonly used for training and evaluating machine learning models in image classification.

No manual download is required—the dataset is fetched automatically when the code runs.